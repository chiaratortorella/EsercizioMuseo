package museo.comparators;

import museo.Quadri;

import java.util.Comparator;

public class ComparatorOrdinamentoPersonalizzato implements Comparator<Object> {
    @Override
    public int compare(Object o1, Object o2) {
        int risultato = 0;
        if(o1 instanceof Quadri){

        }
        return risultato;

    }
}
